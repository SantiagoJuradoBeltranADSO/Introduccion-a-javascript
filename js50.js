// Variables de temperatura y presión
let temperatura = 90;
let presion = 210;

// Sentencia if-else para mostrar "Alarma" o "Normal"
if (presion > 200 || temperatura > 100) {
    console.log("Alarma");
} else {
    console.log("Normal");
}